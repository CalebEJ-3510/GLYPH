# Core dependencies
pandas>=2.0
pyyaml>=6.0
scikit-learn>=1.3
rich>=13.0
joblib>=1.3
lxml>=4.9
numpy>=1.24

# Windows-specific (uncomment when running on Windows with Sysmon)
# pywin32>=306
# python-evtx>=0.7.4

# Dashboard (required — it's the only entry point)
streamlit>=1.28
plotly>=5.17

# Development / testing
pytest>=7.4
pytest-cov>=4.1